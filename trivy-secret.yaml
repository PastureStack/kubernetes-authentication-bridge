allow-rules:
  - id: test-token-literals
    description: Deterministic non-production unit-test token values.
    path: '(_test\.go)$'
    regex: '(test1|another-token|super-secret-token|Bearer (admin|member|secret|upstream-token))'
