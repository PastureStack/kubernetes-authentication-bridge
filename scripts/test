#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
mkdir -p "${repo_root}/dist"
cd "${repo_root}"
go test -mod=mod -race -count=1 -covermode=atomic \
    -coverprofile=dist/coverage.out ./...
