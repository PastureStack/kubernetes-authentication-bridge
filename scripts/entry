#!/usr/bin/env bash
set -euo pipefail

mkdir -p bin dist
if [ "$#" -gt 0 ] && [ -x "./scripts/$1" ]; then
    "./scripts/$@"
else
    exec "$@"
fi

if [[ "${DAPPER_UID:-}" =~ ^[0-9]+$ && "${DAPPER_GID:-}" =~ ^[0-9]+$ ]]; then
    chown -R "${DAPPER_UID}:${DAPPER_GID}" bin dist
fi
